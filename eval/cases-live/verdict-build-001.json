{
  "id": "verdict-build-001",
  "kind": "verdict-build",
  "note": "Scenario 1 — a clean change implementing its spec with all tests passing → the `pass` side of the whole-change verdict. Single-author oracle (the family precedent FAFF-154 set; verdict-build's oracle is the same single-verdict closed-set shape as routing's). The fixture is a self-contained described BuildFixture (Decision: option b), not a real git worktree.",
  "question": "Review this change against its spec and return the whole-change verdict.",
  "fixture": {
    "version": 1,
    "change_summary": "Add a `--json` flag to the `config get` subcommand so a scalar value can be emitted as a JSON-quoted string.",
    "spec": "## WHAT\n`faff config get <key>` prints the resolved scalar as a bare string (exit 0), or DEFAULT/empty + exit 3 when absent. Add `--json`: when passed, the value is emitted JSON-quoted (a string literal) so callers can `JSON.parse` it without ambiguity. The absent case under `--json` prints `null` and keeps exit 3. No other subcommand changes.\n\n## DONE\n- [ ] `config get k --json` prints `\"v\"` (quoted) for a present scalar; exit 0\n- [ ] `config get missing --json` prints `null`; exit 3\n- [ ] the bare (non-`--json`) path is byte-identical to before\n- [ ] a unit test covers both arms",
    "diff": "diff --git a/bin/faff b/bin/faff\nindex 1a2b3c4..5d6e7f8 100755\n--- a/bin/faff\n+++ b/bin/faff\n@@ -210,9 +210,16 @@ function cmdConfigGet(args) {\n-  const { key, def } = parseGetArgs(args);\n+  const { key, def, json } = parseGetArgs(args);\n   const val = resolveKey(key);\n   if (val == null) {\n-    if (def != null) { process.stdout.write(String(def)); process.exit(0); }\n-    process.exit(3);\n+    if (def != null) { process.stdout.write(json ? JSON.stringify(String(def)) : String(def)); process.exit(0); }\n+    if (json) process.stdout.write(\"null\");\n+    process.exit(3);\n   }\n-  process.stdout.write(String(val));\n+  process.stdout.write(json ? JSON.stringify(String(val)) : String(val));\n   process.exit(0);\n }\n@@ -188,6 +188,7 @@ function parseGetArgs(args) {\n   let key, def;\n+  let json = false;\n   for (let i = 0; i < args.length; i++) {\n+    if (args[i] === \"--json\") { json = true; continue; }\n     if (args[i] === \"-d\") { def = args[++i]; continue; }\n     if (key == null) key = args[i];\n   }\n-  return { key, def };\n+  return { key, def, json };\n }\ndiff --git a/test/config-get.test.mjs b/test/config-get.test.mjs\nindex 9f8e7d6..3c2b1a0 100644\n--- a/test/config-get.test.mjs\n+++ b/test/config-get.test.mjs\n@@ -40,3 +40,15 @@ test(\"config get prints a bare scalar\", () => {\n   assert.equal(run([\"config\", \"get\", \"tracking.team_key\"]).stdout, \"FAFF\");\n });\n+\n+test(\"config get --json quotes a present scalar, exit 0\", () => {\n+  const r = run([\"config\", \"get\", \"tracking.team_key\", \"--json\"]);\n+  assert.equal(r.stdout, '\"FAFF\"');\n+  assert.equal(r.status, 0);\n+});\n+\n+test(\"config get --json on a missing key prints null, exit 3\", () => {\n+  const r = run([\"config\", \"get\", \"tracking.nope\", \"--json\"]);\n+  assert.equal(r.stdout, \"null\");\n+  assert.equal(r.status, 3);\n+});\n",
    "test_results": "node --test: all 16 pass (2 new config-get --json tests green; the bare-path test unchanged and passing). Lint clean."
  },
  "oracle": {
    "closed_set": ["pass"]
  }
}
