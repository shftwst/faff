{
  "id": "holdout-seed-neg-spec-satisfying-but-broken-elsewhere-backup-restore-001",
  "kind": "holdout-exercise",
  "label": "defective",
  "defect_class": "spec-satisfying-but-broken-elsewhere",
  "expected_aggregate": "gaps",
  "fixture": {
    "version": 1,
    "spec_dod": [
      {
        "key": "c0",
        "class": "scenario",
        "text": "`dbback backup orders-db` produces a snapshot `snap-2026-01` and `dbback verify snap-2026-01` confirms it is restorable."
      },
      {
        "key": "c1",
        "class": "scenario",
        "text": "`dbback restore snap-2026-01` into a fresh target reproduces `orders-db`'s row counts exactly as they were at backup time."
      },
      {
        "key": "c2",
        "class": "assertion",
        "text": "A backup taken while a write is mid-transaction never captures a partially-committed row (the snapshot is transactionally consistent)."
      },
      {
        "key": "c3",
        "class": "assertion",
        "text": "Restoring `snap-2026-01` into an already-populated target refuses by default (no silent overwrite) unless an explicit `--force` is given."
      }
    ],
    "recordings": [
      {
        "command": "dbback backup orders-db; dbback verify snap-2026-01",
        "output": "verify OK: snap-2026-01 restorable"
      },
      {
        "command": "dbback restore snap-2026-01 into fresh target",
        "output": "row counts match backup time exactly"
      },
      {
        "command": "backup taken during a mid-transaction write",
        "output": "snapshot is transactionally consistent"
      },
      {
        "command": "dbback restore snap-2026-01 into an already-populated target (no --force)",
        "output": "restore proceeded, existing data overwritten"
      }
    ]
  },
  "question": "Derive which of the raw recordings bear on each DoD criterion (ignore any that bear on none), classify each met / unmet / needs-human, and force every `prose` criterion to needs-human.",
  "oracle": {
    "closed_set": [
      "c0:met",
      "c1:met",
      "c2:met",
      "c3:unmet"
    ]
  }
}
