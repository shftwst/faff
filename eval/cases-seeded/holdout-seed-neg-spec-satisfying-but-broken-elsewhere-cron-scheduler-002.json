{
  "id": "holdout-seed-neg-spec-satisfying-but-broken-elsewhere-cron-scheduler-002",
  "kind": "holdout-exercise",
  "label": "defective",
  "defect_class": "spec-satisfying-but-broken-elsewhere",
  "expected_aggregate": "gaps",
  "fixture": {
    "version": 1,
    "spec_dod": [
      {
        "key": "c0",
        "class": "scenario",
        "text": "`hourly-sync` scheduled at `0 * * * *` fires at its next scheduled time and completes successfully."
      },
      {
        "key": "c1",
        "class": "scenario",
        "text": "If `hourly-sync`'s previous run is still executing when the next scheduled time arrives, the scheduler applies its `queue` policy rather than starting a second concurrent run unconditionally."
      },
      {
        "key": "c2",
        "class": "assertion",
        "text": "A run of `hourly-sync` that throws is recorded as `failed`, not silently swallowed as a completed run."
      },
      {
        "key": "c3",
        "class": "assertion",
        "text": "The scheduler's next-run-time calculation for `0 * * * *` accounts for the job's OWN actual last-run time, not a naive fixed-interval guess that could drift."
      },
      {
        "key": "c4",
        "class": "prose",
        "text": "The scheduler's run-history view makes it easy to spot a job that's silently stopped firing."
      },
      {
        "key": "cx1",
        "class": "scenario",
        "text": "An unrelated read-only endpoint touched incidentally by this flow behaves as documented elsewhere (distractor — not part of this case's seeded defect)."
      },
      {
        "key": "cx2",
        "class": "assertion",
        "text": "A concurrent, unrelated request against a different resource is unaffected by this flow (distractor — not part of this case's seeded defect)."
      }
    ],
    "recordings": [
      {
        "log": "[scheduler] hourly-sync fired at scheduled time (0 * * * *)",
        "log2": "run recorded as succeeded"
      },
      {
        "log": "[scheduler] hourly-sync still running; next fire time reached (overlapMode=queue)",
        "log2": "applied queue policy"
      },
      {
        "log": "[scheduler] hourly-sync run threw an exception",
        "log2": "run recorded as failed"
      },
      {
        "log": "[scheduler] hourly-sync last actual run was late by 90s",
        "log2": "next-run time computed from the ORIGINAL schedule, drift compounding"
      },
      {
        "note": "GET /health (unrelated endpoint touched incidentally by this flow)",
        "response": "200 OK; body {\"status\":\"ok\"}"
      },
      {
        "note": "a concurrent, unrelated request against a different resource, observed mid-flow",
        "response": "200 OK; unaffected by this flow"
      }
    ]
  },
  "question": "Derive which of the raw recordings bear on each DoD criterion (ignore any that bear on none), classify each met / unmet / needs-human, and force every `prose` criterion to needs-human.",
  "oracle": {
    "closed_set": [
      "c0:met",
      "c1:met",
      "c2:met",
      "c3:unmet",
      "c4:needs-human",
      "cx1:met",
      "cx2:met"
    ]
  }
}
