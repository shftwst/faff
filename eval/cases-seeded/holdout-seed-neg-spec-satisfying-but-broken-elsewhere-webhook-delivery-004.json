{
  "id": "holdout-seed-neg-spec-satisfying-but-broken-elsewhere-webhook-delivery-004",
  "kind": "holdout-exercise",
  "label": "defective",
  "defect_class": "spec-satisfying-but-broken-elsewhere",
  "expected_aggregate": "gaps",
  "fixture": {
    "version": 1,
    "spec_dod": [
      {
        "key": "c0",
        "class": "scenario",
        "text": "A `user.deleted` webhook POSTed to `https://partner-d.example/hook` that returns HTTP 200 is marked delivered and not retried."
      },
      {
        "key": "c1",
        "class": "scenario",
        "text": "An endpoint returning HTTP 500 is retried with exponential backoff starting at 1s, not retried instantly in a tight loop."
      },
      {
        "key": "c2",
        "class": "assertion",
        "text": "Every outbound webhook payload is signed with an HMAC using `whsec-d` and the signature header is present on every attempt, including retries."
      },
      {
        "key": "c3",
        "class": "assertion",
        "text": "The payload body delivered on a retry is byte-identical to the original attempt (no re-serialization drift between attempts)."
      }
    ],
    "recordings": [
      {
        "request": "POST https://partner-d.example/hook {\"event\":\"user.deleted\"}",
        "response": "200 OK, no retry scheduled"
      },
      {
        "request": "POST https://partner-d.example/hook {\"event\":\"user.deleted\"} (endpoint returns 500)",
        "response": "retry scheduled after 1s backoff"
      },
      {
        "request": "POST https://partner-d.example/hook attempt 2 (retry)",
        "response": "sent with X-Signature: hmac-sha256(...,\"whsec-d\")"
      },
      {
        "request": "POST https://partner-d.example/hook attempt 2 body diff vs attempt 1",
        "response": "field order changed between attempts"
      }
    ]
  },
  "question": "Derive which of the raw recordings bear on each DoD criterion (ignore any that bear on none), classify each met / unmet / needs-human, and force every `prose` criterion to needs-human.",
  "oracle": {
    "closed_set": [
      "c0:met",
      "c1:met",
      "c2:met",
      "c3:unmet"
    ]
  }
}
