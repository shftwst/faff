{
  "id": "holdout-seed-neg-subtly-wrong-cron-scheduler-003",
  "kind": "holdout-exercise",
  "label": "defective",
  "defect_class": "subtly-wrong",
  "expected_aggregate": "gaps",
  "fixture": {
    "version": 1,
    "spec_dod": [
      {
        "key": "c0",
        "class": "scenario",
        "text": "`weekly-cleanup` scheduled at `0 3 * * 0` fires at its next scheduled time and completes successfully."
      },
      {
        "key": "c1",
        "class": "scenario",
        "text": "If `weekly-cleanup`'s previous run is still executing when the next scheduled time arrives, the scheduler applies its `skip` policy rather than starting a second concurrent run unconditionally."
      },
      {
        "key": "c2",
        "class": "assertion",
        "text": "A run of `weekly-cleanup` that throws is recorded as `failed`, not silently swallowed as a completed run."
      },
      {
        "key": "c3",
        "class": "assertion",
        "text": "The scheduler's next-run-time calculation for `0 3 * * 0` accounts for the job's OWN actual last-run time, not a naive fixed-interval guess that could drift."
      },
      {
        "key": "c4",
        "class": "prose",
        "text": "The scheduler's run-history view makes it easy to spot a job that's silently stopped firing."
      }
    ],
    "recordings": [
      {
        "log": "[scheduler] weekly-cleanup fired at scheduled time (0 3 * * 0)",
        "log2": "run recorded as failed despite clean exit"
      },
      {
        "log": "[scheduler] weekly-cleanup still running; next fire time reached (overlapMode=skip)",
        "log2": "applied skip policy"
      },
      {
        "log": "[scheduler] weekly-cleanup run threw an exception",
        "log2": "run recorded as failed"
      },
      {
        "log": "[scheduler] weekly-cleanup last actual run was late by 90s",
        "log2": "next-run time computed accounting for the actual last-run"
      }
    ]
  },
  "question": "Derive which of the raw recordings bear on each DoD criterion (ignore any that bear on none), classify each met / unmet / needs-human, and force every `prose` criterion to needs-human.",
  "oracle": {
    "closed_set": [
      "c0:unmet",
      "c1:met",
      "c2:met",
      "c3:met",
      "c4:needs-human"
    ]
  }
}
