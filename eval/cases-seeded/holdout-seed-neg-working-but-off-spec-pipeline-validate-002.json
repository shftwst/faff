{
  "id": "holdout-seed-neg-working-but-off-spec-pipeline-validate-002",
  "kind": "holdout-exercise",
  "label": "defective",
  "defect_class": "working-but-off-spec",
  "expected_aggregate": "gaps",
  "fixture": {
    "version": 1,
    "spec_dod": [
      {
        "key": "c0",
        "class": "scenario",
        "text": "A record whose `metric_name` matches the declared schema passes `metrics-pipeline`'s validation stage unchanged."
      },
      {
        "key": "c1",
        "class": "scenario",
        "text": "A record missing `metric_name` entirely is routed to the dead-letter output, never the accepted stream."
      },
      {
        "key": "c2",
        "class": "assertion",
        "text": "When the reject ratio for a batch exceeds 2%, `metrics-pipeline` halts the batch and raises an alert instead of continuing to process it."
      },
      {
        "key": "c3",
        "class": "assertion",
        "text": "Records that pass validation retain every original field byte-for-byte in the accepted-stream output (no silent field coercion)."
      },
      {
        "key": "c4",
        "class": "prose",
        "text": "The dead-letter alert's phrasing gives an on-call engineer enough context to act without digging."
      },
      {
        "key": "cx1",
        "class": "scenario",
        "text": "An unrelated read-only endpoint touched incidentally by this flow behaves as documented elsewhere (distractor — not part of this case's seeded defect)."
      },
      {
        "key": "cx2",
        "class": "assertion",
        "text": "A concurrent, unrelated request against a different resource is unaffected by this flow (distractor — not part of this case's seeded defect)."
      }
    ],
    "recordings": [
      {
        "event": "record {\"metric_name\":\"ok-1\"} -> validation",
        "outcome": "accepted, unchanged"
      },
      {
        "event": "record {} (missing metric_name) -> validation",
        "outcome": "routed to dead-letter"
      },
      {
        "event": "batch reject ratio 4% (threshold 2%)",
        "outcome": "batch continued processing, no alert"
      },
      {
        "event": "record {\"metric_name\":\"ok-2\",\"extra\":\"x\"} -> accepted stream",
        "outcome": "output byte-identical to input"
      },
      {
        "note": "GET /health (unrelated endpoint touched incidentally by this flow)",
        "response": "200 OK; body {\"status\":\"ok\"}"
      },
      {
        "note": "a concurrent, unrelated request against a different resource, observed mid-flow",
        "response": "200 OK; unaffected by this flow"
      }
    ]
  },
  "question": "Derive which of the raw recordings bear on each DoD criterion (ignore any that bear on none), classify each met / unmet / needs-human, and force every `prose` criterion to needs-human.",
  "oracle": {
    "closed_set": [
      "c0:met",
      "c1:met",
      "c2:unmet",
      "c3:met",
      "c4:needs-human",
      "cx1:met",
      "cx2:met"
    ]
  }
}
