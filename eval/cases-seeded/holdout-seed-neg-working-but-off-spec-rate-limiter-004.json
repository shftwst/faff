{
  "id": "holdout-seed-neg-working-but-off-spec-rate-limiter-004",
  "kind": "holdout-exercise",
  "label": "defective",
  "defect_class": "working-but-off-spec",
  "expected_aggregate": "gaps",
  "fixture": {
    "version": 1,
    "spec_dod": [
      {
        "key": "c0",
        "class": "scenario",
        "text": "`client-w` making 20 requests to /v1/reports in one minute succeeds on all 20."
      },
      {
        "key": "c1",
        "class": "scenario",
        "text": "`client-w`'s (20 + 1)th request within the same minute is rejected with HTTP 429."
      },
      {
        "key": "c2",
        "class": "assertion",
        "text": "The 429 response carries a `Retry-After` header naming a wait time, not a bare rejection."
      },
      {
        "key": "c3",
        "class": "assertion",
        "text": "The rate-limit counter for `client-w` resets at the minute boundary — a request one second into the next minute is not still counted against the prior window."
      }
    ],
    "recordings": [
      {
        "request": "client-w: 20 req/min to /v1/reports",
        "response": "all 20 requests 200 OK"
      },
      {
        "request": "client-w: request #21 same minute",
        "response": "429 Too Many Requests"
      },
      {
        "request": "429 response headers",
        "response": "no Retry-After header present"
      },
      {
        "request": "client-w: request 1s into next minute",
        "response": "200 OK (fresh window)"
      }
    ]
  },
  "question": "Derive which of the raw recordings bear on each DoD criterion (ignore any that bear on none), classify each met / unmet / needs-human, and force every `prose` criterion to needs-human.",
  "oracle": {
    "closed_set": [
      "c0:met",
      "c1:met",
      "c2:unmet",
      "c3:met"
    ]
  }
}
