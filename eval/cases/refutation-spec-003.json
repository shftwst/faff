{
  "id": "refutation-spec-003",
  "kind": "refutation-spec",
  "fixture": {
    "spec": "WHY: we're locked to Linear. WHAT: support any tracker. HOW: extract a generic tracker interface, port Linear + Jira + GitHub Issues, add a caching layer, and backfill all historical issues — in this one ticket. DONE: all three trackers work and history is migrated.\n\n## Methodology critique\nThis bundles four independently-valuable slices (interface extraction; three provider ports; cache; historical backfill). It is not a right-sized increment, cannot ship or be reviewed incrementally, and the backfill is a one-way migration best isolated."
  },
  "question": "Refute this spec across the enabled lenses; emit objections {lens,severity} or approve.",
  "oracle": {
    "closed_set": [
      "methodology"
    ]
  },
  "_comment": "LEGIT CATCH: the attached Methodology critique flags a too-large, non-incremental slice; the methodology lens must translate that into a blocker → reject-approach (plot re-slices). NOTE this fixture INCLUDES the ## Methodology critique block, because the methodology lens consumes it and degrades to no-signal without it."
}
