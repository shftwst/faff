# Spec — bless-set methodology named-output (slice algorithm + contract)

> producer: faffter-dark-nlspec · adaptor: faffidavit-spec · 2026-06-10 · confidence: high

Optional `bless-set` named-output: not-eligible backlog + dep graph + per-member `faff next --if-eligible`
+ appetite → ranked approvable bless-sets (which not-eligible root to bless, how far down the chain).

## Chosen
- Optional, requested by wtf+tidy, degrade-to-flat-On-hold-list (zero-config safe).
- min unlock-value threshold ≥2 (mirrors value-chains render gate).
- shared member claimed by highest-priority root; other marks "shared with <root>".
- agile override composes structural baseline + re-ranks value×risk (no re-implementation).

## DONE
- Gateway methodology-slot table lists bless-set Optional (wtf+tidy) + In→out + degrade note.
- Structural returns ranked sets: A→B→C not-eligible chain → {A,B,C} in dep order; frontiers (branch/risk/value-plateau).
- Each set carries hypothetical-verdict distribution from faff next --if-eligible.
- lone leaf (<2 unlock) omitted; agile composes+re-ranks; no-methodology → flat list (no regression); validate-adapters passes.
